name: Consumer Alpha Canary

on:
  push:
    branches:
      - optimization
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: consumer-alpha-canary-${{ github.ref }}
  cancel-in-progress: true

jobs:
  baseline:
    permissions:
      actions: read
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/ecosystem-baseline.yml@badeb2367cda4ca96507ddcc0ce9e473ba1ab253
    with:
      require-complete: true
      actions-security-blocking: false
      dependency-blocking: false
      secrets-blocking: false
      iac-blocking: false
      static-analysis-blocking: false
      skip-dirs: node_modules,vendor,target,dist,test/fixtures
