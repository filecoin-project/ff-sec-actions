# Copy to scripts/dev/.env (gitignored) and fill in. Every dev script sources
# scripts/dev/.env automatically if it exists.

# Required by call.sh only. fetch/build/render never touch the Anthropic API.
ANTHROPIC_API_KEY=

# Optional: only needed if `gh auth status` is not already logged in with
# access to the fixture repo.
#GH_TOKEN=
