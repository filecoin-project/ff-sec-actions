name: Ecosystem Baseline (reusable)

on:
  workflow_call:
    inputs:
      actions-security-blocking:
        type: boolean
        required: false
        default: false
      dependency-blocking:
        type: boolean
        required: false
        default: false
      iac-blocking:
        type: boolean
        required: false
        default: false
      static-analysis-blocking:
        type: boolean
        required: false
        default: false
      require-complete:
        description: Fail when any required evaluation is missing, skipped, incomplete, or errored.
        type: boolean
        required: false
        default: true
      skip-dirs:
        type: string
        required: false
        default: node_modules,vendor,target,dist

jobs:
  actions-security:
    permissions:
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/sec-actions.yml@5b2da0e0fda3e9268b69a35451ca016f1b4d24ba
    with:
      blocking: ${{ inputs.actions-security-blocking }}

  dependencies:
    permissions:
      actions: read
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/sec-dependencies.yml@5b2da0e0fda3e9268b69a35451ca016f1b4d24ba
    with:
      blocking: ${{ inputs.dependency-blocking }}
      skip-dirs: ${{ inputs.skip-dirs }}

  secrets:
    permissions:
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/sec-secrets.yml@5b2da0e0fda3e9268b69a35451ca016f1b4d24ba

  infrastructure:
    permissions:
      actions: read
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/sec-iac.yml@5b2da0e0fda3e9268b69a35451ca016f1b4d24ba
    with:
      blocking: ${{ inputs.iac-blocking }}
      skip-dirs: ${{ inputs.skip-dirs }}

  static-analysis:
    permissions:
      actions: read
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/sec-semgrep.yml@5b2da0e0fda3e9268b69a35451ca016f1b4d24ba
    with:
      blocking: ${{ inputs.static-analysis-blocking }}

  profile-conclusion:
    name: Profile Conclusion
    if: always()
    needs: [actions-security, dependencies, secrets, infrastructure, static-analysis]
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Download normalized Evaluation Results
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          pattern: evaluation-result-*
          path: evaluation-results
          merge-multiple: true

      - name: Aggregate Ecosystem Baseline evidence and enforce conclusion
        if: always()
        uses: filecoin-project/ff-sec-actions/actions/aggregate-results@650fdf6412ff2e3e19ac8f4f124a3257f81c19d9
        with:
          results-directory: evaluation-results
          profile-id: ecosystem-baseline
          profile-version: 1.0.0
          required-evaluations: '["zizmor-actions","trivy-dependencies","gitleaks","trivy-iac","semgrep-baseline"]'
          require-complete: ${{ inputs.require-complete }}

      - name: Upload Ecosystem Baseline Evidence Bundle
        if: always() && hashFiles('evidence-bundle.json') != ''
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ecosystem-baseline-evidence
          path: |
            evidence-bundle.json
            evidence-summary.md
