name: Consumer Alpha Canary

on:
  push:
    branches:
      - optimization
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: consumer-alpha-canary-${{ github.ref }}
  cancel-in-progress: true

jobs:
  baseline:
    permissions:
      actions: read
      contents: read
    uses: filecoin-project/ff-sec-actions/.github/workflows/ecosystem-baseline.yml@12c3ea8d24a42b6cf9f335e58e0b21c92db88171
    with:
      require-complete: true
      actions-security-blocking: false
      dependency-blocking: false
      secrets-blocking: false
      iac-blocking: false
      static-analysis-blocking: false
      skip-dirs: node_modules,vendor,target,dist,test/fixtures
