# Local dev harness artifacts: fetched diffs, API responses (may contain
# reviewed code excerpts), and secrets. Never commit these.
scripts/dev/fixtures/
scripts/dev/.env

# Local implementation planning state and implementor claims.
.plan/
